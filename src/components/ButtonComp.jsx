import React from "react";
import Button from "@mui/material/Button";

const ButtonComp = () => {
  return (
    <Button
      variant="contained"
    >
      Accept
    </Button>
  );
};

export default Button;
